# Frozen model bundle

The public release will place the versioned frozen model bundle here, or make
it downloadable from a GitHub release/model repository. Run
`scripts/export_model_bundle.py` inside the research project to create the
bundle locally. The bundle contains eight component weight files,
`thresholds.json`, and `frozen_model_manifest.json`.

